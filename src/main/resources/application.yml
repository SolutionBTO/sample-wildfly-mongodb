server:
  servlet:
    context-path: /api/v1

spring:
  profiles:
    active: default
  devtools:
    add-properties: false
  data:
    mongodb:
      host: localhost
      port: 27017
      database: sampleDB1