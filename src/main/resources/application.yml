server:
  servlet:
    context-path: /

spring:
  profiles:
    active: default
  devtools:
    add-properties: false
  data:
    mongodb:
      host: localhost
      port: 27017
      database: sampleDB

info:
  app:
    version: @project.version@
    name: @project.artifactId@
    description: @project.description@
